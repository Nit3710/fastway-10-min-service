# Netlify Build Configuration for Admin Panel
[build]
  base = "admin-panel"
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "https://fastway-10-min-service.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

