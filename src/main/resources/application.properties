# Server Port
server.port=8080

# Browser admin origin allow-list. Comma-separate explicit production origins; never use '*'.
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174

# Firebase Storage. Enable and provide the service-account JSON path/bucket in deployment.
firebase.enabled=true
firebase.storage.enabled=${FIREBASE_STORAGE_ENABLED:false}
firebase.credentials.path=C:/Users/Nitesh khandelwal/Desktop/mobileApp/config/firebase-service-account.json
firebase.storage.bucket=${FIREBASE_STORAGE_BUCKET:}

# Supabase Storage for admin product images. Keep the service-role key server-side only.
supabase.storage.enabled=${SUPABASE_STORAGE_ENABLED:false}
supabase.url=https://nttirkbfwtelhunbbjxl.supabase.co
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY:}
supabase.storage.bucket=fastway

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# SMTP email notifications. Disabled by default; enable only after configuring a real SMTP account.
spring.config.import=optional:file:./config/application-local.properties
mail.enabled=true
mail.from=niteshkhandelwal127@gmail.com
spring.mail.host=smtp.gmail.com
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=niteshkhandelwal127@gmail.com
spring.mail.password=zkdgmjnpfmiszyvp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/fastway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&useUnicode=true&connectionCollation=utf8mb4_unicode_ci
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.connection.characterEncoding=utf8mb4
spring.jpa.properties.hibernate.connection.useUnicode=true

# JWT Configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.access-expiration=900000
auth.refresh-expiration-days=30
# Google OAuth2 Client Configuration
google.client.id=729828660522-q8o71ci6ltefrv63n53r3943h3nsk04u.apps.googleusercontent.com

# Razorpay Test Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_replace_me}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:replace_me}
# Configure this separately in Razorpay Dashboard -> Account & Settings -> Webhooks.
# Prefer environment variables in deployed environments, for example:
# razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:replace_me}

# Firebase Database Configuration
firebase.database.url=https://web-scrap-9675e-default-rtdb.firebaseio.com/
